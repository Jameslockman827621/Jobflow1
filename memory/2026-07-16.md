# 2026-07-16

## Tsenta gap closure (E2E) — continued hardening

### Proven live (not dry-run)
- Greenhouse fill on real GitLab board: first/last/email/phone + custom Qs (`core_ok`)
- Lever fill on real Wealthfront `/apply`
- Account → start application → `POST /apply-engine/headless` filled **11 fields**, status `needs_user` (no spam submit)
- Discovery: probed 163 → 74 live boards, ~10.3k jobs est., directory 388

### Still weak
- Workday: `/apply` often account-wall / timeout under automation — adapter detects wall
- 50k pages / true real-time / CAPTCHA-solved auto-submit need keys + more import volume

### Tests
- 21 passed including `test_live_ats_fill` + scale 120 apps

Branch: cursor/tsenta-gap-closure-e2e-24ca

### Dashboard / ops packaging (this turn)
- Dashboard Automation strip + headless queue toggle after batch-start
- `/api/v1/health/ready` expanded (DB hard, Redis degraded-ok)
- Dockerfile Playwright Chromium; `.env.example` apply-engine vars
- `scripts/scale_apply_smoke.py`; QUICKSTART + TSENTA-GAP-CLOSURE updated
- Commit: `f01b53c`

### Headless / messaging / adapter gap closure
- `resume_files.py` → `/tmp/jobscale_resumes/`; headless sets `resume_local_path`
- CAPTCHA mock (`TWOCAPTCHA_API_KEY=mock` or `CAPTCHA_MOCK`); status `needs_user` if unsolved
- proxy_pool: `human_delay`, `stealth_init_script`, sticky proxy by user_id hash
- Greenhouse EEO prefer-not-to-say; Lever radios/selects/resume/textareas; Ashby 20s + label fill; Workday adventureButton goto
- Twilio webhook `POST .../messaging/twilio`; APPLY queues `apply_one.delay`; report API for extension
- Celery: prefetch=1, acks_late, rate 30/m; form-filler Ashby + report POST

## Checklist completion (same day, continued)

Worked through full granular checklist without mid-course testing.
Final suite: **27 passed** (unit + e2e + scale + live GH/Lever + webhook/report/captcha mock/health).
Seed catalog: 404 GH / 253 Lever / 150 Workable / 174 Ashby (~1057 rows).
