# 2026-07-16

## Tsenta gap closure (E2E) — continued hardening

### Proven live (not dry-run)
- Greenhouse fill on real GitLab board: first/last/email/phone + custom Qs (`core_ok`)
- Lever fill on real Wealthfront `/apply`
- Account → start application → `POST /apply-engine/headless` filled **11 fields**, status `needs_user` (no spam submit)
- Discovery: probed 163 → 74 live boards, ~10.3k jobs est., directory 388

### Still weak
- Workday: `/apply` often account-wall / timeout under automation — adapter detects wall
- 50k pages / true real-time / CAPTCHA-solved auto-submit need keys + more import volume

### Tests
- 21 passed including `test_live_ats_fill` + scale 120 apps

Branch: cursor/tsenta-gap-closure-e2e-24ca
