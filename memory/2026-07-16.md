# 2026-07-16

## Connect LinkedIn/Indeed → Easy Apply E2E

Shipped product path (not OAuth — LinkedIn has no Easy Apply OAuth scope):

1. Dashboard **Board connections** → Connect LinkedIn/Indeed
2. Extension reads cookies (`li_at` / Indeed auth) → `POST .../board-sessions/{board}/from-cookies`
3. Backend stores Playwright `storage_state` on `BoardSession`
4. Headless Easy Apply loads session; live board hosts without session return `needs_user` + `connect_hint`
5. Login wall after Connect invalidates session → reconnect prompt

Tests: `tests/test_connect_boards_e2e.py` + `tests/test_board_coverage.py` — 16 passed.

Branch: `cursor/tsenta-gap-closure-e2e-24ca`
Doc: `CONNECT-BOARDS-E2E.md`

## World-class SaaS honesty pass

Implemented (no mock success paths):
- Plan-aware apply quotas (free 5/day+month)
- BoardSession Fernet encryption at rest
- AI CV / interview coach fail without OPENAI_API_KEY
- Applications submit requires `?manual=true` or use apply-engine
- Dashboard always loads jobs + polls headless batch status
- Analytics real zeros; Kanban stage updates; 401 logout
- SendGrid path; Stripe subscription.updated; scrape auth
- Extension configurable API/dashboard via options page

Doc: `WORLD-CLASS-SAAS-TODO.md`
Tests: `test_world_class_saas_honesty.py` + connect/scale suites

## Wave 2 — career/reviews/auth/ops

- Career + Reviews pages render real API data (no Stripe/ladder fiction)
- Password reset + email verification (JWT purpose tokens + pages)
- ATS scrape_all_jobs uses curated company lists
- Health `/ready` reports Celery + Playwright; production disables demo seeds
- Referrals page; Premium Stripe checkout; honest landing copy
- Tests: `test_auth_career_wave.py`

## Wave 3 — ops / tracker / answer bank

- Admin-gate `POST /jobs/scrape/{source}` (`is_admin` or `ADMIN_EMAILS`)
- Apify LinkedIn `scrape_company_jobs` / `scrape_all_jobs` call `search_jobs` (no NotImplemented)
- Global rate-limit middleware (Redis + memory fallback); tests disable via `RATE_LIMIT_*=0`
- Alembic `20260716_board_answer`: board_sessions, answer_bank, users.is_admin / auto_apply_submit
- `GET /applications` includes latest `apply_run`; Kanban badges; dashboard reconnect banner
- Answer bank CRUD API + `/answers` UI; `POST /auth/extension-token` + bridge upgrades extension JWT
- Tests: `test_wave3_ops.py` (7 passed)

## Wave 4 — referrals / email / coach / hardening

- Referral claim grants real Pro trial; Stripe customer-balance credit when possible (pending until checkout)
- Invite/links use `APP_URL`; signup `?ref=` claims after register
- Signed unsubscribe tokens + `/unsubscribe` page; email footers no longer `#`
- Production email never succeeds via console-only; CORS/DEBUG hardened in `assert_safe_for_production`
- Interview coach UI + AppShell nav; Kanban HTML5 drag-drop + mobile stage select
- Structlog JSON logging; admin `/apply-engine/metrics/ops`; extension prod host_permissions + optional grants
- Tests: `test_wave4_saas.py`

## Wave 5 — monitor auto-queue + hardening

- `monitor_auto_queue` user opt-in → high-match new monitored jobs → `UserAutoApplyJob`
- Sticky fingerprint concurrency test; log redaction for cookies/tokens; optional `SENTRY_DSN`
- Dashboard toggle for monitor auto-queue
- Tests: `test_wave5_monitor.py`

## Frontend/extension world-class SaaS audit

Audit-only pass (no fixes). Highest-impact gaps:
- Extension hardcoded to localhost; production Connect/auto-apply broken
- Dashboard jobs only load when `has_cached_jobs`; empty otherwise
- Analytics/career/reviews largely hardcoded mock UI; API data ignored
- Kanban read-only (no stage updates); no interview/referrals UI despite marketing
- Default Apply path is fill-not-submit; headless progress not surfaced after queue
