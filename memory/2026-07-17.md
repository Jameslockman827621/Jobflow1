# 2026-07-17

## Auto-apply reliability wave 2

Branch: `cursor/auto-apply-reliability-wave2-24ca` — PR #8

Hardened:
- Progress heartbeats (`last_progress_at`) in `headless_apply`; stale sweeper prefers heartbeat (30m)
- Indeed login wall: strong signals only; fixed Playwright mixed CSS+`text=/` selector bug (was silently False)
- Same selector fix for LinkedIn login wall → session invalidation now works
- Captcha fast-path; submit confirm 2s re-poll
- Edge matrix: Workable, Indeed Apply, Indeed login wall, session invalidate, heartbeat stale spare

Honest claim unchanged: cannot promise live Easy Apply everywhere without Connect + Celery + ToS smoke.

Tests: full `test_auto_apply_edge_matrix.py` cases verified in batches (Playwright cold-start slow in this env).
Doc: `AUTO-APPLY-RELIABILITY.md`
