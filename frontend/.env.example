# Build-time public vars for the Next.js app (static export / production)

# API origin without trailing slash — rewrites in dev proxy to this host
NEXT_PUBLIC_BACKEND_URL=http://localhost:8000

# Or set the full API prefix explicitly (overrides BACKEND_URL composition):
# NEXT_PUBLIC_API_URL=https://api.example.com/api/v1
